/// Developer / debug configuration.
/// Hardcoded flags — change here and restart the app.
class DevConfig {
  DevConfig._();

  /// Set to true to show developer tools in the Settings screen.
  static const bool developerFeaturesEnabled = true;
}
