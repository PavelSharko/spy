# 📦 Архив выполненных задач — ui-agent

> Сюда переносятся промпты из `agent_tasks/current_tasks.md` после выполнения.
> Порядок: новые сверху.

---

## ✅ Рефакторинг карточки игрока + файл визуальных настроек | 2026-03-17

Прочитай перед началом:
- `memory_bank/code_patterns.md`
- `lib/utils/app_styles.dart` (паттерн для констант стиля)
- `lib/widgets/game_card.dart` (метод _buildFrontSide — там вносишь изменения)

**Шаг 1: Создай `lib/utils/visual_config.dart`**

```dart
import 'package:flutter/material.dart';

/// Визуальные константы приложения.
/// Меняй здесь — результат сразу виден по всему проекту.
class VisualConfig {
  VisualConfig._();

  // ── Карточки игрока ──────────────────────────────────────────────────

  /// Непрозрачность картинки-рубашки на карточке.
  /// 0.0 = невидима, 1.0 = полностью видна.
  static const double cardBgImageOpacity = 0.8;

  /// Непрозрачность белого оверлея поверх картинки.
  /// Делает картинку менее яркой чтобы текст роли хорошо читался.
  static const double cardWhiteOverlayOpacity = 0.20;
}
```

**Шаг 2: Измени `lib/widgets/game_card.dart` — метод `_buildFrontSide`**

В `BoxDecoration`:
- УДАЛИ `gradient: LinearGradient(...)` (красный/зелёный фон).
- Вместо него поставь: `color: AppStyles.darkAccent,`

В Stack после `Opacity` с картинкой:
- УДАЛИ `Positioned.fill` с `DotsPatternPainter`.
- Добавь вместо него:
```dart
// White overlay to soften the background image
Positioned.fill(
  child: ColoredBox(
    color: Colors.white.withValues(alpha: VisualConfig.cardWhiteOverlayOpacity),
  ),
),
```

Замени хардкод `opacity: 0.5` на `VisualConfig.cardBgImageOpacity`.

Добавь `import '../utils/visual_config.dart';` вверху файла.

**Шаг 3: Обнови `memory_bank/code_patterns.md`**

Добавь секцию "Визуальные настройки":
- Все визуальные константы (прозрачности, оверлеи) → в `VisualConfig`.
- Цвета палитры → в `AppStyles`.
- Не хардкодить числа прозрачности в виджетах напрямую.

**Шаг 4: Обнови `MEMORY_BANK.md`**

В таблицу файлов добавь:
```
| Изменить прозрачность картинки на карточке | `lib/utils/visual_config.dart` → VisualConfig |
```

**Шаг 5: После выполнения**
- Отметь этот раздел как ✅ Выполнено в этом файле.
- Перенеси полный текст промпта в `agent_tasks/old_prompts_ui_agent.md`.



---
## 🔵 ui-agent | Ожидает выполнения

### Задача: Визуальные элементы для уникальных карточек в меню настроек
1. В `lib/screens/settings_screen.dart` добавь новый переключатель "Уникальные карточки" (аналогично дизайну кнопки "Звук"), управляющий `AppSettings.instance.uniqueCardsEnabled`.
2. Если `uniqueCardsEnabled == true`, покажи под ним две новые настройки:
   - **Стиль карточек**: Область (кнопка). Под надписью текущий стиль. Если дефолт — "как настоящее фото" или "не выбрано".
   - **Персональные карточки с лицами игроков**: Переключатель вкл/выкл, управляющий `AppSettings.instance.playerFacesEnabled`.
3. При нажатии на **Стиль карточек** открывается всплывающее окно (`showDialog`).
   - Размеры окна: отступы 10% от левого и правого края, 15% сверху и снизу экрана. Адаптируется под размер.
   - Внутри прокручиваемый список со стилями: `"реальное фото", "мультяшный", "пиксели", "живопись", "фэнтези", "черно белое", "детское", "18+", "комиксы", "аниме", "киберпанк", "неон", "ретро плакаты ссср", "ретро плакаты сша"`.
   - Окно пропадает при тапе вне области или при выборе стиля.
4. Выбранный стиль сохраняется в `cardStyle` и отображается на экране.



---
## 🔵 ui-agent | Ожидает выполнения

### Задача: Исправление ошибок компиляции в SettingsScreen и верстки
1. **Ошибка Color**: В `lib/screens/settings_screen.dart` (строки 282 и 299) используется `AppStyles.primary`, которого не существует в `lib/utils/app_styles.dart`. Замени его на `AppStyles.accent`.
2. **Ошибка Overflow**: В логах терминала зафиксирована ошибка `A RenderFlex overflowed by 907 pixels on the bottom` в `lib/screens/pre_game_flow_screen.dart` (виджет `Column-[<'cardReveal_0'>]`). 
   - Тебе нужно обернуть содержимое карточки (или саму колонку) в `SingleChildScrollView` или использовать `Expanded`/`Flexible` для элементов, чтобы они вписывались в экран при появлении клавиатуры или на маленьких устройствах.
   - Проверь `pre_game_flow_screen.dart` на наличие жестко заданных высот внутри `Column`.



---
## 🔵 ЭТАП 2: ui-agent (Фотокамера и Вывод результатов)

### Цель: Создать интерфейс захвата фото и отобразить аватарки/финальные карточки.

1. **Экран Камеры**: Создай виджет `CameraOverlay` в `lib/widgets/`. 
   - Рамка-овал с ушами.
   - Отступы: 15% (верх/низ), 10% (бока).
   - Кнопка "Сфоткать мордаху".
   - Используй пакет `camera` (предварительно проверь разрешения).
2. **Интеграция в Flow**: В `lib/screens/pre_game_flow_screen.dart` добавь новый `FlowStep.photoCapture`. Этот шаг идет сразу после того, как игрок ввел/выбрал имя.
   - Если `playerFacesEnabled == false`, этот шаг пропускается.
3. **Аватарки**: На экранах `RoundScoreScreen` и `VotingScreen` добавь `CircleAvatar` рядом с именем каждого игрока, используя `player.photoBytes`.
4. **Финальные карточки**: На экране `RoundScoreScreen` (или там, где объявляется победитель раунда), вместо стандартного фона или рядом с текстом, подставь сгенерированную карточку из `session.roundFinalCards` в зависимости от того, выиграл шпион или нет.



---
## 🔵 ui-agent | Статус: ⏳ Ожидает выполнения

### Задача: Адаптация UI под новые цвета
*(Стартует ПАРАЛЛЕЛЬНО или СРАЗУ ПОСЛЕ code-agent)*
1. **Аудит интерфейса**: Проверь, как новые цвета из `AppStyles` (светло-желтый на глубоком темно-кэжуал синем фоне `#2c3e50`) смотрятся на основных экранах (Диалоговые окна, меню, карточки).
2. **Исправление контраста**: Если после изменения цветов `code-agent`-ом кнопки или тексты стали сливаться с фоном, поправь прозрачность, добавь тени или поменяй вызов переменной `AppStyles`, чтобы текст `#ebc462` всегда был читаемым. Возможные места для проверки: `cardBg`, тексты внутри `SettingsScreen`, таймеры.
3. **Полировка**: Убедись, что после удаления полосок из фона игра всё так же выглядит стильно и нет пустых/сломанных контейнеров.

**После выполнения:** отметь ✅ и перенеси промпт в `agent_tasks/old_prompts_ui-agent.md`.



---
## 🔵 ui-agent | Статус: ⏳ Ожидает выполнения

### Задача: Проверка верстки после удаления `const` и оберток
*(Стартует ПОСЛЕ code-agent)*
1. **Аудит интерфейса**: `code-agent` только что удалил глобальную обертку `AnimatedPatternBackground`. Твоя задача — проверить экраны. Убедись, что на экранах (например `rules_screen`, `settings_screen`) фоновый цвет по-прежнему прокрашивается правильно (через `Scaffold(backgroundColor: AppStyles.primaryBg)` или фоновые контейнеры). Если какие-то экраны остались с белым/черным дефолтным фоном из-за исчезновения обертки — добавь там `backgroundColor: AppStyles.primaryBg`.
2. **Контраст по правилам цветового круга**: Убедись, что динамически вычисленные `code-agent`ом цвета (cardBg, darkAccent) действительно создают нужную глубину на экране согласно принципам `gradients.app`. Если карточки сливаются, напиши об этом или сам слегка измени коэффициенты `.lerp` в `AppStyles.dart` для лучшего выделения.

**После выполнения:** отметь ✅ и перенеси промпт в `agent_tasks/old_prompts_ui-agent.md`.



---
## 🔵 ui-agent | Статус: ⏳ Ожидает выполнения

### Задача: Дизайн-интеграция экрана настроек игры

*(Стартует ПОСЛЕ code-agent)*

Экрану настроек (`lib/screens/game_settings_screen.dart`) нужно придать стилистику начальной страницы, используя новые динамические компоненты.

**Шаги:**
1. Скомпонуй экран `GameSettingsScreen.dart`, используя только виджеты из `lib/widgets/common/` (созданные `code-agent`) и переменные из `lib/utils/app_styles.dart`.
2. Заголовок `AppStrings.gameSettingsTitle` ("НАСТРОЙКИ ИГРЫ") должен стоять на том же уровне и иметь тот же вес, что и заголовок на главном экране. 
3. Настрой элементы выбора (количество игроков, шпионов, время) так, чтобы они не выглядели как дефолтные Flutter-виджеты. Используй для активных состояний акцентный цвет `AppStyles.accent`.
4. Убедись, что навигация "Назад" вписана в дизайн экрана и не ломает композицию. Фоновый цвет должен быть `AppStyles.primaryBg`. Никаких жестких привязок (хардкода).
5. Визуально экран должен смотреться как часть одной игры вместе с `main_menu_screen.dart`.

**После выполнения:** отметь ✅ и перенеси промпт в `agent_tasks/old_prompts_ui-agent.md`. Обязательно вызови скрипт архивации.


---
## 🔵 ui-agent | Статус: ⏳ Ожидает выполнения

### Задача: Применение нового цвета к тексту настроек

*(Стартует ПОСЛЕ code-agent)*

Элементы выбора на экране настроек (Количество игроков, Время игры, Количество раундов, Выбор локации) имеют внутри себя текст названия, текст выбранного значения (или "не выбрано"), выпадающее меню и кнопки `+` / `-`. Все эти текстовые элементы должны быть перекрашены.

**Шаги:**
1. Открой экран настроек (`GameSettingsScreen` или `lib/screens/settings_screen.dart` / `game_round_screen.dart` в зависимости от того, где находятся 4 кнопки меню).
2. Пройдись по всем 4 виджетам/кнопкам, которые отвечают за:
   - `AppStrings.playerCount`
   - `AppStrings.gameTime`
   - `AppStrings.roundCount`
   - `AppStrings.locationSelection`
3. У каждого из этих элементов измени цвет текста:
   - Сам заголовок (например, "Количество игроков")
   - Текущее значение или надпись "не выбрано"
   - Текст внутри выпадающего меню (если есть)
   - Иконки или кнопки `+` и `-` внутри этих плашек.
4. Все эти элементы должны использовать цвет `AppStyles.settings_game_text_colors` (который `code-agent` только что создал).

**После выполнения:** отметь ✅ и перенеси промпт в `agent_tasks/old_prompts_ui-agent.md`. Обязательно вызови скрипт архивации.



---
## 🔵 ui-agent | Статус: ⏳ Ожидает выполнения

### Задача: Применение нового цвета в меню локаций

*(Стартует ПОСЛЕ code-agent)*

Нужно применить только что созданный цвет к конкретным кнопкам списка локаций, не трогая главную экшен-кнопку.

**Шаги:**
1. Найди экран или виджет, который отвечает за меню "Выбор локации" (скорее всего это `lib/screens/location_selection_screen.dart` или виджет списка локаций внутри экрана игры).
2. Выдели все кнопки, которые отображают названия самих локаций (обычные элементы списка выбора). 
3. Замени цвет текста внутри этих кнопок на новую переменную `AppStyles.location_menu_button_text_color`.
4. **Важное исключение**: Найди кнопку "Подтвердить" / "Далее" (кнопка действия, фиксирующая выбор локации). **Её цвет текста менять НЕ НАДО**. Оставь её в текущем дизайне.

**После выполнения:** отметь ✅ и перенеси промпт в `agent_tasks/old_prompts_ui-agent.md`. Обязательно вызови скрипт архивации.

